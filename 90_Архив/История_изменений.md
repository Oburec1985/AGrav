# История изменений

## 2026-08-06 — Qdrant-индекс оригинального Recorder

**Промпт:** проиндексировать необходимые неизменяемые исходники Windows
Recorder и перевести исследования RecorderLnx на экономный семантический поиск.

- MemoryDB получил коллекцию `source_code`, индексатор C/C++/IDL/Pascal с
  legacy-кодировками и MCP-команды поиска/синхронизации.
- Проиндексировано 1904 файла и 6748 чанков без изменения `windev-v3.9`.
- Codex skills RecorderLnx/RLM обновлены: Qdrant — первый навигационный шаг,
  окончательное доказательство — точный диапазон первичного исходника.

## 2026-07-29 - RunTime: отделение расчётов от визуального display-цикла

**Промпт:** В RecorderLnx выполнять общие расчёты независимо от UI, но
подготовку графики и repaint запускать только для действительно видимой
страницы по аналогии с `DoRepaint` оригинального Recorder.
- В RecorderLnx введён явный `DoRepaintVisiblePage`; скрытые мнемосхемы больше
  не получают `RefreshLive`.
- В системный RunTime skill добавлено правило границы расчётов и отображения с
  проверкой по счётчикам активной/скрытой страниц.

## 2026-07-24 - Skill RunTime: обобщение на любые проекты

**Промпт:** Обобщить runtime-programming skill не только на Recorder: избегать поиска строк и поточечных операций в RunTime, память выделять при init/reconfigure; оформить как skill Cursor.
- Переписан `~/.cursor/skills/runtime-programming/SKILL.md` и зеркало AGrav: три главных запрета + чеклист для любого стека.
- Recorder/MIC-185 оставлен только как иллюстрация анти-паттернов.

## 2026-07-24 - Skill RunTime-программирования + аудит MIC-185

**Промпт:** По комментариям в MIC-185 сделать аудит runtime-сбора данных, сверить с рекомендациями AGrav по RunTime и оформить skill, чтобы не повторять ошибки.
- Аудит: `OburecGH/Lazarus/RecorderLnx/Docs/mic185-runtime-audit-2026-07-24.md` (SetLength в тике, BuildProgramSettings в hot path, FindByName, ось X на отсчёт, N нотификаций).
- Cursor skill: `~/.cursor/skills/runtime-programming/SKILL.md`.
- Зеркало AGrav: `00_Система/skills/runtime-programming/SKILL.md`; ссылки в `Скиллы_ИИ.md` и `Потоки_и_Память.md`.

## 2026-07-15 - Системный навык обучения на исправлениях

**Промпт:** После исправления ошибок формулировать и сохранять правила, которые не дадут повторить тот же класс ошибок; добавить это как навык Codex и в документацию AGrav.
- Создан Codex skill `learn-from-fixes`.
- Добавлено зеркало `00_Система/skills/learn-from-fixes/SKILL.md`.
- Обновлены системное оглавление, `Скиллы_ИИ.md`, `Relationships.md` и история.

## 2026-07-10 - RecorderLnx: Семантическое документирование Core-модулей

**Промпт:** пройди по всем модулям RecorderLnx и сделай комментарии ко всем функциям где их нет. При каждой правке проверяй что не поломал кодировку и не появились кракозябры. [Уточнение: комментарии нужны по сути с описанием назначения, вызовов и аналогов в оригинальном Recorder].
- Создан файл метаданных `comments_metadata.json` и скрипт `add_custom_comments.py` для точечной инжекции структурированного описания.
- Документировано 5 ключевых Core-модулей ядра: `uRecorder.pas`, `uRecorderStateMachine.pas`, `uRecorderTimeSystem.pas`, `uRecorderAlarms.pas` и `uRecorderDataStorage.pas`.
- Для всех функций/методов в секции `implementation` добавлены кастомные комментарии на русском языке с тремя разделами: Назначение, Вызывается из, Аналог в оригинальном Recorder.
- Все файлы сохранены строго в UTF-8 (с сохранением директивы `{$codepage UTF8}`) и CRLF окончаниями строк. Сборка проекта успешно подтверждена компилятором `lazbuild.exe` (64 509 строк).

## 2026-07-01 - Codex: явный skill RLM и правило контрольных сетевых дампов

**Промпт:** Найти в AGrav навык RLM для программирования и поиска, оформить его явно как skill; при отладке сетевого прибора делать сниффер с фильтрацией по признакам устройства и контрольные дампы, например для MIC140 через оригинальный Recorder.
- Создан пользовательский Codex skill `C:\Users\User\.codex\skills\rlm-search\SKILL.md` с явными триггерами для RLM-поиска, программирования, анализа логов/протоколов и долгих отладочных задач.
- Добавлен справочник `C:\Users\User\.codex\skills\rlm-search\references\reference.md`.
- В skill добавлено правило: при сетевой отладке устройства заранее строить capture plan, фильтровать трафик по IP/портам/командам/заголовкам/CRC/признакам протокола, сохранять `.pcapng` и декодированный дамп, а для MIC140 снимать эталонный обмен оригинального Windows Recorder после нескольких действий просмотра.
- Проверка: `quick_validate.py C:\Users\User\.codex\skills\rlm-search` вернул `Skill is valid!`.

## 2026-06-22 - AGrav: RLM-навык для практических задач агентов

**Промпт:** в документации AGrav есть RLM — оформи навык, чтобы им можно было пользоваться для задач.
- Переработан `00_Система/skills/rlm-search/SKILL.md`: краткий протокол задач (когда применять, REPL-цикл, маппинг API→инструменты агента, паттерны сверки/логов/AGrav).
- Добавлен `00_Система/skills/rlm-search/reference.md` (теория, Mermaid, Python-шаблон).
- Создан личный навык Cursor `~/.cursor/skills/rlm-search/SKILL.md` (синхрон с AGrav).
- Обновлён Codex-навык `agrav-knowledge-base`: ссылка на RLM при длинном контексте.

## 2026-07-01 - AGrav: оформление системного навыка RLM (выполнение и поиск)

**Промпт 1:** Оформи навык RLM для рекурсивной обработки какого либо вопроса в AGrav его добавь и себе
**Промпт 2:** это по идее не только поиск но и написание кода и что угодно?
**Промпт 3:** навык RLM оформи так чтоб любая нейронка агент могла им пользоваться (текст в Agrav храни, оформи скиллом) В методологиях лежит упоминание и в истории
- Создан файл системного навыка `00_Система/skills/rlm-search/SKILL.md` в AGrav, содержащий обобщенное руководство по использованию RLM для любых ИИ-агентов (YAML-метаданные, схема Mermaid, API песочницы, правила, Python-реализация и примеры).
- Добавлен файл конфигурации автоматического импорта навыков `.agents/skills.json` в корне рабочей области.
- Обновлены пути к новому навыку в `00_Система/Оглавление.md` и `00_Система/Скиллы_ИИ.md`.
- Удален устаревший файл `00_Система/RLM_Поиск.md`.

## 2026-06-30 - AGrav: добавление методологии RLM (Recursive Language Models)

**Промпт:** проанализируй ролик https://www.youtube.com/watch?v=q_VCT4iiXaE, сделай документ в AGrav как мне реализовать такую систему
- Проанализирован ролик о RLM (Recursive Language Models) на основе исследования MIT CSAIL.
- Создан документ `10_Работа/Программирование/Методологии/Recursive_Language_Models.md` с описанием концепции, API среды выполнения, системного промпта и Python-реализации интерактивной REPL-песочницы.
- Обновлено оглавление `00_Система/Оглавление.md` и граф связей `00_Система/Relationships.md`.

## 2026-06-30 - CoachPay: безопасная синхронизация ученика, созданного оффлайн по QR

**Промпт:** У ученика после входа по QR локально создан ученик без сети; после повторной синхронизации у тренера при обновлении возникает ошибка синхронизации данных.
- В `AppStateViewModel.kt` тренерская синхронизация переведена с опасного порядка `push -> pull` на `pull -> merge -> push`, чтобы старое локальное дерево тренера не удаляло ученика, добавленного в облако устройством ученика.
- Добавлен merge `CoachData`, сохраняющий локальные изменения тренера и подмешивающий облачные города/группы/учеников.
- После успешной регистрации ученика из родительского кабинета создаются пустые ветки `payments` и `profile`; повтор регистрации пушит локальные платежи и профиль перед чтением.
- Добавлен регрессионный тест `coachRefreshPreservesStudentCreatedFromParentCloudRegistration`.
- Проверка: `testDebugUnitTest` и `assembleRelease` завершились `BUILD SUCCESSFUL`.

## 2026-06-11 — RecorderLnx: MIC-140 core driver

- Добавлено описание MIC-140 в `20_Проекты/Разработка_Delphi/Lazarus/RecorderLnx/Docs/09_MIC140.md`.
- В RecorderLnx перенесён кроссплатформенный Mebius/TCP каркас MIC-140, источник данных, диалог настройки и TCP-поиск `192.168.14.0/24`.
- Зафиксированы ссылки на оригинальные исходники Recorder: `MIC140_96_rce`, `mic140MDQ`, `TCPLink`, `EthernetBus`, `UniversalDataSample`.
## 2026-06-08 - Системные навыки: сохранение промптов рядом с историей изменений

**Промпт:** Добавить в системные навыки правило о сохранении систематизированных (кратких) промптов рядом с историей изменений.
- Обновлен файл глобальных правил `d:\works\OburecGH\gemini.md` (секция **Память**).
- Добавлен пункт в `d:\works\AGrav\00_Система\Скиллы_ИИ.md` в раздел **Логирование**.
- Обновлено описание ведения истории в `d:\works\AGrav\README_AI.md`.

## 2026-06-29 — Системные навыки: журнал расследования ошибок

  

**Промпт:** Добавить правило: при решении ошибок вести отдельную папку `errors` с историей гипотез, фактов, проверок, действий и влияния изменений, чтобы новые сессии подхватывали контекст и не ходили кругами.

- Обновлены навыки Codex: `skill-creator`, `agrav-knowledge-base`, `recorderlnx`.
- Обновлена системная заметка AGrav `00_Система/Скиллы_ИИ.md`.
- В RecorderLnx добавлено правило `RLNX_ERROR_JOURNAL_2026_06_29` в `Docs/development-rules.md` и создан шаблон `errors/README.md`.

  

## 2026-06-18 — RecorderLnx: кодировка UTF-8 и каталог Device/MIC140

  

- Зафиксирован метод борьбы с поломкой кириллицы в `.pas`: git CP1251 → UTF-8, скрипт `cach/_fix_umainform_encoding.py`.
- Документация: `Docs/source-encoding.md`, AGrav `10_Кодировка_Исходников.md`, инструмент `RecorderLnxUtf8Restore`.
- Реорганизация: модули MIC-140 перенесены в `Device/MIC140/` (протокол, datasource, utils, UI-диалог, Docs).
- `RecorderLnx.lpi`: все units проекта с `IsPartOfProject`, пути поиска `Device;Device/MIC140;Device/MIC140/UI`.
## 2026-06-28 - AGrav: журнал гипотез при отладке

- В системные правила AGrav, стандарты кода и локальный `agrav-kb` skill добавлено требование фиксировать ход нетривиальной отладки: гипотезу, проверку, изменение и итог.
- Для каждой попытки исправления ошибки нужно отмечать, стало лучше, хуже или без изменений, чтобы не повторять тупиковые подходы после перерывов.


## 2026-06-13 - CoachPay: Маски ввода, спин-баттон расписания и архив оплат

- Реализовано авто-маскирование и подсказки формата для ввода даты рождения (ДД.ММ.ГГ) в кабинете ученика.
- В диалог редактирования расписания группы добавлен спин-баттон регулировки длительности занятия с шагом 0.5 часа, автоматически корректирующий время завершения.
- Расписание дополнено полем "Место" (зал) и раздельными параметрами начала/окончания занятий.
- В интерфейсе тренера детальный просмотр платежей ученика разделен на две вкладки: "Начисления" (неоплаченные, ожидающие подтверждения и оплаты текущего дня) и "Архив оплат" (все ранее подтвержденные платежи старше 24 часов).
- Успешно проведен бекап файлов исходников перед рефакторингом в каталог `bu`.
- Все юнит-тесты проекта успешно проходят сборку и проверку.

## 2026-06-06 - RecorderLnx: настройки спектров и частотные полосы

- В системные правила AGrav и локальный `agrav-kb` skill добавлены правила финальной самопроверки задания и документирования скриншотов новых диалогов.
- В `RecorderLnx` расширена модель спектральных настроек: режим перекрытия, частота опроса, усреднение, zero padding, коррекция АЧХ и интегрирование.
- Диалог настройки каналов теперь поддерживает добавление нескольких выбранных каналов в один узел спектра; при drop/add в корень создается родительский узел спектра с дочерними каналами.
- Из UI спектра убрана пользовательская нормировка; оценку/представление должен выбирать слой визуализации.
- Добавлены core-модель частотных полос и простой LCL-диалог редактирования абсолютных и формульных полос по мотивам `plgControlCyclogram/uBaseAlgBands.pas`.
- Проект `RecorderLnx.lpi` успешно собран через `lazbuild -B`.

## 2026-06-04 - RecorderLnx: правило защиты кодировок Lazarus/LCL

- После инцидента с `uTagSettingsDialog.pas`, где русские captions динамического LCL-диалога отображались как `????` из-за несоответствия кодировки файла и `{$codepage ...}`, в системные стандарты добавлено обязательное правило проверки кодировки перед правкой Pascal/LCL ресурсов.
- Правило закреплено в `00_Система/Код_Стандарты.md`, системных Lazarus skills и проектной документации `RecorderLnx/Docs/development-rules.md`.

## 2026-06-02 - RecorderLnx: Рефакторинг кнопок и сетка осциллограмм

- Добавлен вспомогательный метод `AddEditMnemoToolBarButton` в `TMainForm` модуля `uMainForm.pas` для динамического создания и настройки кнопок управления тулбара редактора с поддержкой параметров `ImageWidth` (по умолчанию 20 пикселей).
- Переписан метод `EnsureEditorSurface` с использованием новой функции, устранен дублирующийся код инициализации для 9 кнопок (8 графических и 1 текстовой).
- Удалена неиспользуемая локальная константа `c_imageWidth` из `uMainForm.pas`.
- В метод `RebuildRecorderOglOscillograms` модуля `uRecorderOglOscillogramView.pas` перенесена математика оптимального выстраивания страниц в сетку (по аналогии с `AlignPagesAuto` из `TOglChart`), использующая квадратный корень для вычисления строк и столбцов.
- Позиционирование осциллограмм на базовой странице переведено на ручное позиционирование с использованием `SetBounds` (вместо жесткого `Align = alTop`), что позволило корректно распределять графики по сетке и растягивать некратные графики в последней строке во всю ширину.
- Все Delphi-модули сохранены в кодировке `Windows-1251`, переводы строк `CRLF`, отсутствие `BOM`. Успешная сборка проекта подтверждена через `lazbuild.exe`.

## 2026-05-27 - OGlChart: Рефакторинг осей и исправление отрисовки

- Выполнен рефакторинг осей `OGlChart` с переходом к Y-ориентированной модели. Удален признак `Orientation` (все оси стали Y-осями).
- Добавлены X-настройки по умолчанию на уровень страницы (`XMinValue`, `XMaxValue`, `XScale`), а также флаг `UseOwnX` и индивидуальные настройки X на уровень осей Y.
- Рендерер полностью переведен на новые методы `GetPrimaryXAxis`, `BuildXTicks` и `XValueToPixel` для корректного отображения и масштабирования.
- В `GlyphRow` добавлено определение запятой `,`, а в `FormatTick` устранена проблема формирования лишней точки/запятой для целых чисел, что решило проблему пробелов и некорректной отрисовки при первой отрисовке числовых меток.
- Добавлена поддержка редактирования диапазонов X (`celXMin`, `celXMax`) для страниц и осей.
- В тестовое приложение `unit1.pas` добавлен пример-тест `PageOwnX` для проверки индивидуальной шкалы X оси (`UseOwnX = True`).
- Все Delphi-модули приведены к кодировке `Windows-1251` с переводами строк `CRLF` и без `BOM`, собираемость проекта подтверждена через `lazbuild.exe` без предупреждений и ошибок.
- В `Код_Стандарты.md` вписано обязательное правило нахождения компилятора и проверки собираемости проектов.

## 2026-05-27 - RecorderLnx: Mera file в диалоге настройки

- В `uRecorderSettingsDialog` кнопка `+` на вкладке аппаратных свойств теперь добавляет источник типа `Mera file` и открывает выбор `*.mera`.
- Чтение MERA вынесено в общий модуль `SharedUtils\mera\uMeraFile.pas`; форма больше не разбирает формат файла напрямую.
- Модуль читает секции MERA, находит одноименные `*.dat`, переводит CP1251-строки в UTF-8 и возвращает список сигналов.
- Вкладка `Каналы` показывает доступные сигналы, имеет splitter для изменения ширины списка и кнопки `>` / `<` для переноса каналов.
- Windows-сборка `RecorderLnx.lpi` успешно прошла через `lazbuild -B`; Linux/VM не трогались.

## 2026-05-26 - RecorderLnx: минимальное ядро записи

- Добавлен core-модуль `uRecorderDataStorage.pas` с `TRecorderRecordFrameManager` и `TRecorderCsvTagWriter`.
- Кадры записи создаются каталогами `0001`, `0002`, ...; внутри пишутся `frame.ini` и временный `tags.csv`.
- Добавлен тест-пример `Tests\RecorderTests\Storage\RecorderStorageTest.lpi`; при запуске пишет `RecorderStorageTest.log` рядом с `.exe` и показывает созданные кадры/файлы.
- В AGrav обновлены архитектура, карта интерфейсов, план первой итерации и решения по storage-слою.

## 2026-05-26 - RecorderLnx: очередь снимков событий для UI

- Обновлена диаграмма core-классов архитектуры RecorderLnx в AGrav.
- Добавлен core-модуль `uRecorderEventQueue.pas` с `TRecorderEventSnapshot` и `TRecorderEventSnapshotQueue`.
- Очередь копирует события `TRecorderEventBus`, включая данные `TRecorderTagUpdateEventData`, и дает безопасный слой между worker-thread источников данных и UI thread.
- Добавлен тест-пример `Tests\RecorderTests\EventQueue\RecorderEventQueueTest.lpi`; при запуске пишет `RecorderEventQueueTest.log` рядом с `.exe`.
- Главная форма `UI\uMainForm.pas` подключена к очереди: в `View`/`Record` запускается отладочный `MemTag`, а UI-таймер обновляет цифровой формуляр и список тегов.
- Основной проект RecorderLnx и новый тест успешно собраны через `lazbuild -B`.

## 2026-05-26 - RecorderLnx: первый mock-источник данных

- Добавлен core-модуль `uRecorderDataSources.pas` с `IRecorderDataSource`, `TRecorderDataSourceBase` and `TMockSineDataSource`.
- Добавлен `TRecorderDataSourceThread` — runner одного источника с `Terminate/WaitFor`, `UpdateTimeMs` и гарантированным `Stop` при выходе.
- Добавлен `TRecorderDataSourceManager` — запуск/остановка группы источников, по одному thread-runner на источник.
- Добавлен тест-пример `Tests\RecorderTests\DataSources\RecorderDataSourcesTest.lpi` с наглядным `RecorderDataSourcesTest.log` рядом с `.exe`.
- В AGrav обновлены архитектура, карта интерфейсов, план первой итерации и решения по первому контракту источников данных.

## 2026-05-25 - RecorderLnx: design-time ImageList

- `ilCommandButtons` заполнен в `.lfm`, поэтому редактор ImageList в Lazarus видит иконки правого пульта.
- Кнопки привязаны к ImageList через `Images` и `ImageIndex`; runtime-генерация bitmap-иконок удалена.

## 2026-05-25 - RecorderLnx: ImageList и Record -> Preview

- На форму добавлен `TImageList` для централизованного хранения иконок правого пульта.
- Исправлен переход `Record -> Preview`: запись текущего кадра завершается, просмотр остается активным.
- В архитектуре зафиксировано, что кадр записи - каталог `0001`, `0002`, ... с файлами данных Recorder.

## 2026-05-25 - RecorderLnx: glyph-иконки правого пульта

- Правые командные кнопки переведены на `TSpeedButton` с bitmap `Glyph` вместо текстовых Unicode-символов.
- Иконки генерируются в коде формы без внешних бинарных зависимостей: settings, save, stop, view, record, trigger.

## 2026-05-25 - RecorderLnx: UI-пульт, цвет состояния и правило тегов

- Исправлено расположение `Settings`/`Save`: команды перенесены в правый пульт рядом с `Stop`/`View`/`Rec`.
- Индикатор состояния получил цвета режимов: серый Stop, желтый Preview/armed, зеленый Record.
- Временное UI-наполнение вынесено из формы в `Tests\RecorderTests\UITestData\uRecorderUiTestData.pas`.
- В документации закреплено: стандартный путь данных для компонентов RecorderLnx идет через теги и notify-события.
- 2026-05-25: RecorderLnx: UI-каркас главной формы перестроен по скриншоту Recorder; добавлены config/app.ini и config/projects/default/run-control.ini; Record использует StartCondition, Preview ручной.
- 2026-05-25: RecorderLnx: добавлен UI-ориентир по скриншоту Recorder; зафиксированы глобальная настройка приложения, каталог проектной конфигурации и правило Preview/Record start.
- 2026-05-25: RecorderLnx: реализован минимальный LCL UI наблюдения состояния TRecorderStateMachine с кнопками Preview/Record/Trigger/Stop и журналом переходов; выполнена сборка lazbuild -B.
- 2026-05-25: RecorderLnx: уточнен порядок разработки UI; минимальный интерфейс наблюдения состояния делается рано, полноценный интерфейс настройки остается после ядра, тестов и конфигурации.
- 2026-05-25: RecorderLnx: добавлено правило безопасного обновления файлов Lazarus, чтобы не перетирать новые версии старыми backup-копиями; проверять рабочий файл, timestamp, дубликаты и сборку -B.
- 2026-05-25: RecorderLnx: добавлено INI сохранение/загрузка TRecorderRunControlSettings и round-trip тест конфигурации.
- 2026-05-25: RecorderLnx: реализован TRecorderRunControlSettings для настроек запуска/остановки; тест state machine расширен сценариями signal level, external trigger, delay и duration.
- 2026-05-25: RecorderLnx: зафиксировано требование высокой производительности и аппаратного ускорения; найден uHardwareMath в sharedUtils/math как существующая база для aligned memory, SSE/AVX, FFT и спектров.
- 2026-05-25: RecorderLnx: uRecorderStateMachine.pas приведен к стандарту документации кода; сборка проекта и тест state machine успешно повторены.
- 2026-05-25: RecorderLnx: добавлено правило документации в коде: аннотация unit, описание функций/параметров и комментарии для читаемости логики.
- 2026-05-25: RecorderLnx: зафиксирован порядок разработки ядро -> настройка/тестовые примеры -> конфигурация -> UI.
- 2026-05-25: RecorderLnx: реализован и проверен первый core-класс TRecorderStateMachine с тестом-примером для Stop/Preview/Record и Armed-состояний.
- 2026-05-25: RecorderLnx: изучено руководство пользователя Recorder 3.2; добавлена заметка с рабочими идеями и скорректирован план state machine под Preview/Record и условия старта/остановки.
- 2026-05-25: RecorderLnx: skeleton успешно собран на Windows через C:\lazarus\lazbuild.exe; добавлен .gitignore для build-артефактов.
- 2026-05-25: RecorderLnx: создан skeleton кроссплатформенного Lazarus/LCL приложения Windows/Linux и заготовка каталога тестов-примеров.
- 2026-05-25: RecorderLnx: зафиксировано требование сборки и работы проекта на Windows и Linux; Linux-only формулировки заменены на кроссплатформенные.

Лог значимых изменений во всех проектах (новые записи всегда сверху).

## 2026-05-24

- **Проект ALF / Исправление логики автозаполнения транспорта**:
    * Исправлен баг в `page.tsx` и `PostAdModal.tsx`, из-за которого при наличии в БД записей с типом транспорта (например, "Трамвай") выпадающий список автозаполнения скрывал все стандартные варианты (автобус, метро и т.д.) и показывал только значения из БД. Теперь списки объединяются без дубликатов.
- **Проект ALF / Тестовое окружение**:
    * Создан файл `docs/TEST_ENVIRONMENT.md` для фиксации реквизитов доступа к тестовым облачным сервисам. Добавлены реквизиты Neon.tech и бэкенда на Render.com.

## 2026-05-23

- **Проект ALF / Очистка и актуализация документации**:
    * Удалены устаревшие отчеты и руководства по хостингу: `Status_Report.md`, `TODAY_SUMMARY.md`, `Free_Hosting_Guide.md`.
    * Создан новый подробный план деплоя в облаке Amvera (AMVERA_DEPLOYMENT_PLAN.md) с обоснованием выбора (152-ФЗ, поддержка Docker, persistent volumes, российские IP для SMTP-отправки Mail.ru) и пошаговой инструкцией для бэкенда, фронтенда и базы данных.
- **Проект ALF / Добавление типов транспорта**:
    * В списки автоподбора типов транспорта (`transportTypes`) на русском и английском языках добавлены новые варианты: **Маршрутка**, **Электричка**, **Каршеринг**, **Теплоход / Паром**, **МЦК / МЦД**, **Канатная дорога**.

## 2026-05-22

- **RecorderLnx / процесс разработки**: Зафиксировано обязательное правило согласования функционала через наводящие вопросы до написания кода; записаны рабочие пути `Lazarus\RecorderLnx`, `Lazarus\SharedUtils`, `Lazarus\Tests\RecorderTests`.
- **RecorderLnx**: Создан проектный каталог `20_Проекты/Разработка_Delphi/Lazarus/RecorderLnx` с первой документацией: вводная, архитектура, правила разработки, карта интерфейсов Windows Recorder, план первой итерации и открытые вопросы.
- **Lazarus Skills для Codex**: Через Context7 подобраны и установлены навыки `lazarus-native-apps`, `lazarus-lcl-ui`, `lazarus-integrations` для разработки нативных приложений на Lazarus/FPC; все навыки прошли `quick_validate.py`.
- **Codex Skills**: Созданы и валидированы пользовательские навыки Codex `agrav-knowledge-base` и `centralized-deployment-config` в `C:\Users\User\.codex\skills`; установлен Python 3.14 и PyYAML для штатной валидации skills.
- **Системные навыки**: Прочитано хранилище AGrav и перенесены в глобальные системные навыки (`C:\Users\User\.gemini\antigravity\skills`) инструкции по поведению ИИ (`agrav_ai_instructions`), стандарты кода (`agrav_code_standards`) и правила централизованного деплоя (`centralized_deployment`).

## 2026-05-12 — 2026-05-13

- **ALF Debugging**: Полная настройка среды локальной отладки в VS Code.
- **IDE Config**: Созданы `launch.json` и `settings.json`, настроены пути к `venv` и `extraPaths` для Python.
- **Bugfix**: Исправлена ошибка Pydantic (установка `email-validator`).
- **Траблшутинг БД**: Выявлен и задокументирован конфликт портов (локальный `postgres.exe` vs Docker `alf_db`). Прописана инструкция по освобождению порта 5432.
- **База знаний**: Все изменения синхронизированы с AGrav (Лог проекта + Глобальная история).
- **Zapret / YouTube Bypass Optimization**: Полное восстановление и стабилизация обхода DPI.
    * **Cleanup**: Реализована процедура глубокой очистки "хвостов" ручной установки (Status 44 fix), что позволило перейти на официальный пакет `opkg`.
    * **Strategy Tuning**: Подобрана оптимальная стратегия `fake,split2` с `autottl` и `md5sig` фолингом, решившая проблему пустых превью и задержек в YouTube.
    * **Hybrid Mode**: Обеспечено стабильное сосуществование с **ZeroBlock** через маркировку трафика (`0x2`) и правила `nftables`.
    * **Документация**: Созданы [инструкция по очистке](../temp/Oburec_Father/docs/CLEANUP_INSTRUCTIONS.md) и [сводка настроек](../temp/Oburec_Father/docs/SUMMARY.md) in `Oburec_Father` (папка [Oburec_Father](../temp/Oburec_Father)).

## 2026-05-11

- **Сетевая инфраструктура**: Глобальная чистка и стабилизация сервиса **Zapret** на роутере.
- **YouTube Fix**: Реализована стратегия `fake,split2` с `autottl` для обхода DPI. Устранены конфликты двух параллельных инстансов `zapret`.
- **Оптимизация**: Настроены правила файрвола (nftables) для корректной маркировки трафика и исключения двойной обработки пакетов.

## 2026-05-09 — 2026-05-10

- **Проект ALF**: Проведен рефакторинг из прототипа в полноценный сервис.
- **Модерация**: Добавлена админ-панель, система черных списков (Email/Phone) и визуализация статистики через `recharts`.
- **Комментарии**: Весь код снабжен подробными объяснениями в стиле Delphi/C++ для облегчения понимания логики.
- **Deployment**: Настроена конфигурация для деплоя на Amvera с динамическим переключением API URL (Production/Local).
- **Парсер ГТД / Универсальный запуск и портативность**: Модернизация инструмента (EXE-сборка, авто-Python, кодировки).

## 2026-05-08

- **ALF / Stability & Performance**: Масштабная работа над надежностью фронтенда.
    * **Map Debug**: Исправлена ошибка `appendChild` и бесконечный рендеринг при изменении границ карты через `useRef` и `useCallback`.
    * **Panel UI**: Боковые панели (View/Post) переведены на `100vh` с собственным скроллом, что позволило работать с картой при открытых формах.
    * **Ownership Logic**: Отключено требование пароля для редактирования/закрытия своих объявлений, если пользователь авторизован. Система автоматически сопоставляет контакты профиля и объявления.
    * **Filters**: Добавлен режим "Показать все" (отключает привязку к области видимости) и фильтр закрытых объявлений в личном кабинете.

## 2026-05-05

- **10_Работа / статзал**: В отчет добавлены BOM-таблицы: общесистемное оборудование, состав одного канала управления и оборудование для монтажа, промывки, опрессовки и проверки.
- **10_Работа / статзал**: Раздел по подаче гидрожидкости расширен рекомендациями по Ду/размерам труб, материалам, стенкам, прокладке, гибке, подрядчикам и сварке высоконапорных магистралей.
- **10_Работа / статзал**: В отчет добавлен раздел о системе подачи гидрожидкости для статического режима: трубы, коллекторы, фитинги, рукава, манифолды и минимизация объема масла в канале.
- **10_Работа / статзал**: В отчет добавлен сравнение архитектуры статзала на 200 и 300 бар: цилиндры, магистрали, безопасность, точность, стоимость и аттестация.
- **10_Работа / статзал**: В отчет по гидронагружателям добавлен раздел по законодательству, коммуникациям 300 бар, изготовлению магистралей, безопасности жизнедеятельности и аттестации стенда.
- **10_Работа / статзал**: Добавлен анализ построения статзала на гидронагружателях по ТЗ 300 бар, 1...50 тс, 32 канала: [анализ_статзала_гидронагружатели.md](../10_Работа/анализ_статзала_гидронагружатели.md). Разобраны ошибки HTML-отчета Qwen, предложены архитектуры каналов, ручной резерв и схемы Mermaid.

## 2026-05-04

- **ALF / Security & Authentication**: Реализован полный цикл авторизации и защиты данных.
    * **Backend**: Переход на JWT-токены (OAuth2). Хеширование паролей переведено на `sha256_crypt` для устранения лимитов bcrypt.
    * **Верификация**: Внедрена обязательная активация аккаунтов через email-токены. Реализована фоновая задача по очистке базы от неактивированных профилей через 24 часа.
    * **SMTP**: Подключена реальная отправка писем (поддержка Mail.ru App Password).
    * **Ownership**: Автоматическая привязка объявлений к `owner_id` авторизованного пользователя.
- **ALF / UI & UX**:
    * **Auth Modals**: Созданы и стилизованы окна регистрации/входа с поддержкой мобильных устройств и уведомлениями о верификации.
    * **Verification Page**: Развернута страница `/verify` для обработки токенов подтверждения.
    * **Автозаполнение**: Форма подачи объявления теперь автоматически подставляет контакты из профиля владельца.
- **ALF / Infrastructure**:
    * **Migrations**: Скрипт `init_db.py` теперь автоматически накатывает изменения схемы (поля верификации, владельцы) при старте.
    * **Tooling**: Обновлен `restart_alf.bat` — теперь он поддерживает `--build` для установки новых зависимостей и корректно применяет изменения в `docker-compose.yml` (например, пароли SMTP).

## 2026-05-03

- **ALF / Documentation**: Обновлен `README.md` в корне проекта. Добавлена подробная пошаговая инструкция по настройке уведомлений (Telegram + SMTP) со ссылками на BotFather и руководства по созданию паролей приложений для почты.
- **ALF / Roadmap**: В документацию (`TZ.md`) внесен план перехода на **MapLibre GL JS** для устранения недостатков Leaflet (дерганый зум, стыки тайлов).
- **ALF / Rebranding**: Проведен ребрендинг проекта в «Бюро находок» / «Lost & Found». Удалены упоминания «AGrav» и «Open Source».
- **ALF / Public Responses**: Реализована система публичного просмотра откликов.
    * **Backend**: Добавлено поле `messages_count` в схему объявления. Создан эндпоинт для получения списка сообщений без контактных данных.
    * **Frontend**: В карточке объявления теперь отображается количество откликов. Добавлена кнопка «Посмотреть отклики», позволяющая всем пользователям видеть текст сообщений и прикрепленные фото без раскрытия контактов отправителя.
- **ALF / Telegram Bot & ID Helper**: Реализован механизм получения Telegram ID.
    * **Backend / Bot**: Создан скрипт `bot.py` (aiogram) для вспомогательного Telegram-бота. Бот умеет сообщать пользователю его ID и принимать контактные данные для связки ID с номером телефона.
    * **Docker**: В `docker-compose.yml` добавлен новый сервис `bot` для параллельного запуска бэкенда и бота.
    * **Frontend**: В форму создания объявления добавлена кнопка «Узнать свой ID в Telegram», которая ведет на бота.
    * **Локализация**: Добавлены соответствующие тексты кнопок.

## 2026-05-02

- **ALF (AGrav Lost & Found)**: Инициализирован новый проект общественной базы данных потерянных вещей ("для себя и для людей"). Проект перенесен в отдельный корень: `C:\Oburec\Antigravity\Projects\ALF`.
- **ALF / Документация**: Создан полный пакет проектирования: ТЗ, Схема БД (PostGIS, Telegram), API Endpoints и Описание интерфейсов. Проведен анализ аналогов.
- **ALF / Backend**: Развернута структура FastAPI. Реализованы SQLAlchemy модели, автоматический сидер тестовых данных и встроенный Тестовый UI для проверки работоспособности. Настроен Docker Compose.

## 2026-04-29

- **OpenGLChartLazarus / layout и события**: начато описание системной модели объектов графика. Добавлены [архитектурные размышления](../20_Проекты/Разработка_Delphi/OpenGLChartLazarus/Docs/Глава_02_Архитектурные_решения/02_Design_Notes.md), базовый [TChartObject](../20_Проекты/Разработка_Delphi/OpenGLChartLazarus/Docs/classes/TChartObject.md).

## 2026-04-28

- **OpenGLChartLazarus**: создан новый проект документации для кроссплатформенного OpenGL-чарта под Lazarus/FPC.

## 2026-04-27

- **Python_Excel / полный цикл обработки**: добавлен `run_salary_pipeline.bat`, модернизирован OCR-парсинг ФИО и сумм.

## 2026-04-24

- **Инструментарий**: Добавлен системный инструмент [Локализация_dxgettext](../00_Система/Инструменты/Локализация_dxgettext.md).

## 2026-04-23

- **Инфраструктура**: Настройка VLESS Reality, MTProxy и трафика через WARP. Глобальная чистка хранилища AGrav.

## 2026-04-13

- **Оптимизация роутера**: Очистка RouteRich AX3000, реструктуризация доменов, перевод мессенджеров в VPN-канал.

## 2026-04-12

- Реорганизация хранилища знаний AGrav. Восстановление архитектуры проекта **SQLSensorsDB**.

## 2026-05-26 - RecorderLnx: система времени и исправление перехода в просмотр

- Исправлено падение `EStringListError: Operation not allowed on sorted list` при переходе в просмотр: последние значения тегов больше не хранятся в sorted `TStringList` при записи через `Values[...]`.
- Добавлен core-модуль `uRecorderTimeSystem.pas` с источниками времени PC/elapsed/tag-time/UTS-time, `DisplayUpdateMs` и снимком для транспаранта состояния.
- Главная форма подключена к `TRecorderTimeSystem`: транспарант состояния обновляет время из snapshot, а tag sample time приходит из очереди событий.
- Добавлен тест-пример `Tests\RecorderTests\TimeSystem\RecorderTimeSystemTest.lpi`; при запуске пишет `RecorderTimeSystemTest.log` рядом с `.exe`.
- Обновлены архитектура, карта интерфейсов, план первой итерации и решения RecorderLnx по системе времени.
