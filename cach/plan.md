# План текущих доработок (AGrav)

## История промтов и изменений
* **Промт:** Попытка запушить agrav репозиторий в GitHub. Обнаружено зависание интерфейса из-за файла `model_optimized.onnx` (235 МБ) в истории коммитов.
  * **Изменения:** Подготовлен план сброса истории через `git reset --soft origin/main` для исключения кэша модели.

## Задачи
- [x] Запросить разрешение на запуск git команд (git push).
- [x] Выполнить сброс `git reset --soft origin/main`.
- [x] Добавить `model_cache` в `.gitignore`.
- [x] Убрать `model_cache` из index (`git restore --staged`).
- [x] Сделать коммит без модели.
- [/] Выполнить `git push origin main`.
- [ ] Проверить статус репозитория (git status).
