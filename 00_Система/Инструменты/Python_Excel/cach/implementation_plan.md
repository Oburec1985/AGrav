# План: Исправление пропусков и добавление словаря имен

## Проблема
1. В `result.csv` попадает меньше данных, чем ожидается (некоторые люди пропускаются, так как не найдена строка с итоговой суммой).
2. Ошибки OCR в ФИО мешают сопоставлению с Excel.

## User Review Required
> [!IMPORTANT]
> Для коррекции имен мне нужен эталонный список сотрудников. Я создам файл `employees.txt` в папке проекта. Пожалуйста, проверьте его и дополните актуальными именами.

## Предложенные изменения

### Python_Excel

#### [MODIFY] [parse_zp_image.py](file:///e:/Oburec/AGrav/00_Система/Инструменты/Python_Excel/parse_zp_image.py)
- **Улучшение поиска сумм**:
    - Использовать `difflib` или просто `fuzziness` для поиска "ИТОГО К ВЫПЛАТЕ" (например, "ИТОГО К ВЬІПЛАТЕ", "ИТОГО К ВЫГШАТЕ").
- **Коррекция имен**:
    - Добавить функцию `correct_name(name, dictionary)`, которая использует `difflib.get_close_matches`.
- **Логирование пропусков**:
    - Если человек найден, но сумма нет — выводить WARNING в лог.

#### [NEW] [employees.txt](file:///e:/Oburec/AGrav/00_Система/Инструменты/Python_Excel/employees.txt)
- Список ФИО (по одному в строке) для эталонного сравнения.

## План верификации
1. Запустить скрипт и проверить лог на наличие предупреждений о пропущенных суммах.
2. Проверить `result.csv` на наличие исправленных имен (например, "Скрипник А-р.А." -> "Скрипник А.А.").
