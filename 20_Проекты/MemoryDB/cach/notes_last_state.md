# Состояние работы над проектом MemoryDB

## 1. Проделанные изменения:
- Создан новый проект `MemoryDB` по пути `d:\works\AGrav\20_Проекты\MemoryDB` для организации локальной векторной памяти ИИ-ассистента.
- Реализованы модули:
  - `src/config.py`: пути к файлу БД, коллекции и локальному кэшу модели `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`.
  - `src/embeddings.py`: генерация эмбеддингов через `fastembed` (с локальной загрузкой модели из `model_cache` без обращения к HuggingFace).
  - `src/database.py`: локальный Qdrant Client (Offline-режим). Переведен на API `query_points` для совместимости с qdrant-client 1.18.0.
  - `src/main.py`: MCP сервер с поддержкой stdio-транспорта.
- Написаны проверочные скрипты:
  - `src/test_local.py`: добавление записи и семантический поиск.
  - `run_memory_server.bat`: запуск MCP.
  - `run_test.bat`: запуск автономных тестов.
- Создана документация:
  - `docs/README.md`: установка и регистрация MCP.
  - `docs/architecture.md`: описание двухуровневой памяти.
- Выполнено скачивание файлов модели MiniLM в папку `d:\works\AGrav\20_Проекты\MemoryDB\model_cache` через hf-mirror.com с поддержкой докачки (HTTP Range).
- Зарегистрирован MCP-сервер `local-memory-db` в глобальном файле настроек `C:\Users\User\.gemini\antigravity-ide\mcp_config.json`.
- Протестирован и верифицирован локальный запуск (`test_local.py`). Семантический поиск успешно отработал по кириллическому запросу про Lazarus/LCL.
- Оптимизирована организация памяти (быстрая/долговременная), создана инструкция для LLM по работе с YAML Frontmatter заметок в Obsidian и исключению дублирования. Реализована утилита автоочистки src/cleanup.py от битых ссылок и семантических дубликатов.
- Выполнено слияние дубликатов документации чарта (папка OglChart перенесена в OpenGLChartLazarus/Docs/ и удалена).
- Проиндексированы 6 документов RecorderLnx и OpenGLChartLazarus с встраиванием YAML Frontmatter (memory_id) и подсчетом экономии токенов (в 43 раза).

---

## 2. Промпты, по которым получены изменения (Memory Log):
1. **Промпт**: Создать оффлайн-базу для организации памяти (вариант Б) и добавить новый проект в AGrav.
   *Изменение*: Инициализирован проект `MemoryDB` с файловой векторной базой Qdrant Client + FastEmbed. Созданы исходные коды, тесты, документация и скрипты запуска.
2. **Промпт**: Возобновить работу над проектом, скачать модель эмбеддингов и запустить тест базы данных.
   *Изменение*: Написан надежный скрипт докачки файлов с зеркала HuggingFace (hf-mirror.com). Модель сохранена локально. Библиотека fastembed настроена на оффлайн загрузку. Исправлена ошибка `QdrantClient object has no attribute 'search'` путем перехода на `query_points`. Тесты успешно пройдены. Настройки MCP сервера прописаны в глобальный конфиг IDE.
3. **Промпт**: Возможность разделения БД на множество мелких файлов для удобного коммита в GitHub.
   *Изменение*: Реализована схема, где источником правды являются текстовые JSON-файлы в `data/records/`. Бинарная БД Qdrant синхронизируется с ними при инициализации и добавлена в `.gitignore`.
4. **Промпт**: Объяснить в документации устройство хранения, назначение ONNX модели 220МБ, локальной БД и процессы изменения/удаления.
   *Изменение*: Обновлена документация docs/architecture.md с детальным описанием пофайловой структуры, работы Qdrant, роли model_optimized.onnx и логики CRUD/синхронизации.
5. **Промпт**: Оптимизировать организацию памяти (разделение памяти, Obsidian YAML разметка, очистка дубликатов, инструкция для LLM).
   *Изменение*: Добавлены разделы в docs/architecture.md, создана системная инструкция docs/llm_memory_instructions.md, реализована утилита src/cleanup.py (и run_cleanup.bat) для поиска битых ссылок и семантических дубликатов на базе косинусной схожести векторов.
6. **Промпт**: Перенести файлы OglChart в OpenGLChartLazarus, проиндексировать документы RecorderLnx/OpenGLChartLazarus с YAML-разметкой и оценить экономию токенов.
   *Изменение*: Убрана папка OglChart, файлы перенесены в OpenGLChartLazarus/Docs/. Проведена пакетная индексация 6 документов через скрипт scratch/index_docs.py с автогенерацией YAML Frontmatter в файлах и подсчетом экономии токенов (в 43 раза).




